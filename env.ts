/**
 * File to help keep all ports and URLs in one place
 */

export const HOST_NAME_DEV: string = "http://localhost";

export const CLIENT_PORT_DEV: number = 3000;
export const CLIENT_URL_DEV: string = `${HOST_NAME_DEV}:${CLIENT_PORT_DEV}`;

export const SERVER_PORT_DEV: number = 5000;
export const SERVER_URL_DEV: string = `${HOST_NAME_DEV}:${SERVER_PORT_DEV}`
