import React from 'react'
import { type } from 'os'

type Props = {

}

const Logout = (props: Props) => {
    return (
        <div>
            This is the logout page
        </div>
    )
}

export default Logout
