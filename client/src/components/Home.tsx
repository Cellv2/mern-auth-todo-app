import React from "react";

type Props = {};

// TODO: Do I need this? Can just use the ToDoContainer ( / Form?) straight under the layout
const Home = (props: Props) => {
    return (
        <div>
            <h1>Home route k bois</h1>
        </div>
    );
};

export default Home;
